#!/usr/bin/env bash
set -euo pipefail

echo "Repository-managed secrets are disabled."
echo "Use local .env files instead (see .env.example)."
echo "This script is deprecated and will exit without changes."
exit 1
